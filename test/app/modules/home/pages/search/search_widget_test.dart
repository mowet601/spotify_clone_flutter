void main() {
//  testWidgets('SearchWidget has message', (tester) async {
//    await tester.pumpWidget(buildTestableWidget(SearchWidget()));
//    final textFinder = find.text('Search');
//    expect(textFinder, findsOneWidget);
//  });
}
