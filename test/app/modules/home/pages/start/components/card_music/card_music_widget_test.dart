void main() {
//  testWidgets('CardMusicWidget has message', (tester) async {
//    await tester.pumpWidget(buildTestableWidget(CardMusicWidget()));
//    final textFinder = find.text('CardMusic');
//    expect(textFinder, findsOneWidget);
//  });
}
