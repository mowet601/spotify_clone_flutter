import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:spotify_clone_flutter/app/modules/home/home_module.dart';

void main() {
  initModule(HomeModule());
  // LibraryController library;
  //
  setUp(() {
    //     library = HomeModule.to.get<LibraryController>();
  });

  group('LibraryController Test', () {
    //   test("First Test", () {
    //     expect(library, isInstanceOf<LibraryController>());
    //   });

    //   test("Set Value", () {
    //     expect(library.value, equals(0));
    //     library.increment();
    //     expect(library.value, equals(1));
    //   });
  });
}
