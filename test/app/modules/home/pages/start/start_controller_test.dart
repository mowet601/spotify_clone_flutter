import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:spotify_clone_flutter/app/modules/home/home_module.dart';

void main() {
  initModule(HomeModule());
  // StartController start;
  //
  setUp(() {
    //     start = HomeModule.to.get<StartController>();
  });

  group('StartController Test', () {
    //   test("First Test", () {
    //     expect(start, isInstanceOf<StartController>());
    //   });

    //   test("Set Value", () {
    //     expect(start.value, equals(0));
    //     start.increment();
    //     expect(start.value, equals(1));
    //   });
  });
}
