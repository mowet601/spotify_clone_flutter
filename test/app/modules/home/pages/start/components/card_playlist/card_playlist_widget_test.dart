void main() {
//  testWidgets('CardPlaylistWidget has message', (tester) async {
//    await tester.pumpWidget(buildTestableWidget(CardPlaylistWidget()));
//    final textFinder = find.text('CardPlaylist');
//    expect(textFinder, findsOneWidget);
//  });
}
