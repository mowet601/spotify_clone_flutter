import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('PlayMusicPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PlayMusicPage(title: 'PlayMusic')));
    //  final titleFinder = find.text('PlayMusic');
    //  expect(titleFinder, findsOneWidget);
  });
}
