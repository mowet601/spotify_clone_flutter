import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('LibraryPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(LibraryPage(title: 'Library')));
    //  final titleFinder = find.text('Library');
    //  expect(titleFinder, findsOneWidget);
  });
}
