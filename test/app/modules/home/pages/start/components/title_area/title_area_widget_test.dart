void main() {
//  testWidgets('TitleAreaWidget has message', (tester) async {
//    await tester.pumpWidget(buildTestableWidget(TitleAreaWidget()));
//    final textFinder = find.text('TitleArea');
//    expect(textFinder, findsOneWidget);
//  });
}
